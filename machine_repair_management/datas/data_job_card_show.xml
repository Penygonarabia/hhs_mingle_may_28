<?xml version="1.0" encoding="utf-8"?>
<odoo>


 <record id="action_show_job_card_server" model="ir.actions.server">
        <field name="name">Show Job Cards on Partner Change</field>
        <field name="model_id" ref="machine_repair_management.model_machine_repair_support"/>
        <field name="state">code</field>
         <field name="code">action = False
if record.phone:
    try:
        action = record.action_show_job_card()
    except Exception as e:
        print("Error in action_show_job_card:", str(e))
action
		</field>
    </record>

    <!-- Automated Action to Trigger Server Action on Record Update -->
<!--     <record id="auto_action_show_job_card" model="base.automation">
        <field name="name">Auto Show Job Cards on Partner Change</field>
        <field name="model_id" ref="machine_repair_management.model_machine_repair_support"/>
        <field name="state">code</field>
        <field name="trigger">on_write</field>
        <field name="code">action = False
if record.phone:
    try:
        action = env.ref('machine_repair_management.action_show_job_card_server').run()
    except Exception as e:
        print("Error in automated action:", str(e))
action
        </field>
    </record> -->
</odoo>