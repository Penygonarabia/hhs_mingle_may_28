from . import models
# from . import hooks
#
# def post_init_assign_group(cr, registry):
#     # Called after module installation to assign the group to existing users
#     # Implementation is in hooks.py
#     hooks.post_init_assign_group(cr, registry)
