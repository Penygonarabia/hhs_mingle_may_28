<!-- <odoo>
    <!-- Server Action for Print Contract Document -->
    <record id="action_server_print_contract_document" model="ir.actions.server">
        <field name="name">Print Contract Document</field>
        <field name="model_id" ref="model_promoter_showroom"/>
        <field name="state">code</field>
        <field name="binding_model_id" ref="model_promoter_showroom"/>
        <field name="binding_type">action</field>
        <field name="code">
action = env["promoter.showroom"].browse(env.context.get("active_ids")).action_print_contract_document()
        </field>
    </record>
</odoo> -->
