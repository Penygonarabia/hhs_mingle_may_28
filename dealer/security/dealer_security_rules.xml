<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data noupdate="1">

    <!-- ============================================================
         ir.attachment access rules for Dealer module
    ============================================================ -->

    <!-- Backoffice users can read ALL attachments
         (needed to view invoice attachments uploaded by mobile users) -->
    <record id="rule_attachment_backoffice_read" model="ir.rule">
      <field name="name">Dealer Backoffice: Read All Attachments</field>
      <field name="model_id" ref="base.model_ir_attachment"/>
      <field name="domain_force">[(1, '=', 1)]</field>
      <field name="groups" eval="[(4, ref('dealer.group_dealer_backoffice_user'))]"/>
      <field name="perm_read" eval="True"/>
      <field name="perm_write" eval="False"/>
      <field name="perm_create" eval="False"/>
      <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Mobile users can read attachments they created
         or attachments linked to their own sales records -->
    <record id="rule_attachment_dealer_user_read" model="ir.rule">
      <field name="name">Dealer Mobile User: Read Own Attachments</field>
      <field name="model_id" ref="base.model_ir_attachment"/>
      <field name="domain_force">
        ['|',
          ('create_uid', '=', user.id),
          ('res_model', '=', 'dsales.showroom.sales')
        ]
      </field>
      <field name="groups" eval="[(4, ref('dealer.group_dealer_user'))]"/>
      <field name="perm_read" eval="True"/>
      <field name="perm_write" eval="False"/>
      <field name="perm_create" eval="False"/>
      <field name="perm_unlink" eval="False"/>
    </record>

  </data>
</odoo>
